{
    "description": {
        "default": "Load direnv environment variables at the start of each session."
    },
    "hook": {
        "antigravity": {
            "matcher": "*",
            "type": "command",
            "command": "direnv export json 2>/dev/null | jq '{\"systemMessage\": \"Environment loaded via direnv\"}'"
        },
        "claude": {
            "matcher": "startup",
            "type": "command",
            "command": "eval \"$(direnv export bash 2>/dev/null)\""
        },
        "cursor": {
            "type": "command",
            "command": "eval \"$(direnv export bash 2>/dev/null)\""
        }
    }
}
