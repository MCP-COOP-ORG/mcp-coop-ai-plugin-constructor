{
    "description": {
        "default": "Run git status check when the agent completes a task to show uncommitted changes."
    },
    "hook": {
        "claude": {
            "matcher": "*",
            "type": "command",
            "command": "git status --porcelain 2>/dev/null | head -20"
        },
        "cursor": {
            "type": "command",
            "command": "git status --porcelain 2>/dev/null | head -20"
        }
    },
    "visibility": true
}
